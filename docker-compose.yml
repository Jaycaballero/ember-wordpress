version: '2'
services:
  web:
    build:
      context: ./
    image: eu.gcr.io/neso-io/sx-ember-wordpress
    ports:
      - 4000:4000
  cache:
    image: jonbaldie/varnish:latest
    volumes:
      - ./varnish:/etc/varnish
    ports:
      - 8080:80

